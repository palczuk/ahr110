# AHR 110 — Unit 1 addition

Esta pasta contém uma página principal baseada no `index.html` fornecido e o `script.js` original enviado.

## Arquivos novos
- `unit1.html` — Unit 1 completa, 1.1–1.13 + quiz de 25 perguntas.
- `questions.json` — banco das 25 questões.
- `styles.css` — estilo da página principal fornecida.
- `index.html` — página principal com apenas o novo link lateral UNIT 1.
- `script.js` — script original enviado pelo usuário.

## Importante
O `script.js` original busca `data.json` para reconstruir os tópicos 2–11. Como o `data.json` original não foi enviado nesta mensagem, não foi inventado nem substituído.

Ao publicar, mantenha o `data.json` original do seu repositório junto com estes arquivos.

A Unit 1 é independente e abre por:
`unit1.html`
